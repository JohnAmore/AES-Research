mod sub;

// Substituion
// Substitute each byte with the corresponding hex value.

struct Sub_box {
    sub_box: Vec<u8>,
}

fn subByte(val: String, table: Sub_box) {
    let byte: u8 = val as u8;
}
